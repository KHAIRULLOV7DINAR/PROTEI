# PROTEI
Репозиторий для домашних заданий IT-школы ПРОТЕЙ.  
  
Для работы с json-форматом используется библиотека nlohmann-json3-dev:  
  
sudo apt update  
sudo apt-get install nlohmann-json3-dev  
  
Для тестирования используется фреймворк google.test  
  
Необходимо изначально запустить сервер, а затем клиент!  
  
Для запуска сервера программы (в каталоге DEVELOP/server):  
  
mkdir build  
cd build  
cmake ..
cmake --build .  
./SERVER
  
Для запуска клиента программы (в каталоге DEVELOP/client):  
  
mkdir build  
cd build  
cmake ..  [-DBUILD_TESTS=ON] //для сборки с тестами  
cmake --build .  
ctest -V                    //запуск тестов
./CLIENT -a 127.0.0.1 -p 1100 -L myLib //миниальный набор аргументов для запуска  
  

HOMETASK_01:  
1. Для сборки программы используется cmake, CMakeLists.txt находится в корневой папке проекта;  
2. Программа принимает аргументы командной строки вида:  
HOMETASK_01 -a 127.0.0.1 -p 5555 -r Client -i 0 -L mylib  
3. Программа реализует текстовый интерфейс через меню (команды не чувствительны к регистру):  
3.1. Ввод строкового псевдонима программы (строковое значение)     - name;  
3.2. Ввод типа вектора (строковое значение)                        - type;  
3.3. Ввод числового четырёхмерного вектора (int, double, float)    - vector;  
3.4. Выход                                                         - exit;  
3.5. Вывод аргументов командной строки                             - console;  
3.6. Вывод аргументов, получаемых из меню                          - menu;  
  
HOMETASK_02:  
1. Добавлены необходимые тесты с использованием googletest;  
2. Было добавлено логирование ошибок согласно шаблону;  
3. Был выбран единый способ возврата ошибок (exception);  
4. Был создан список ошибок, который приведен ниже по файлу;  
5. Конфигурация проекта была собрана с cppcheck, возникшие предупреждения были учтены, после исправлений их не осталось;    
6. Проект был собран с адресным санитайзером, ошибок обнаружено не было (результаты в папке screenshots).  
  
Критичные ошибки:  
Отсутствие аргументов ip-адреса и названия библиотеки в аргументах консольной команды;  
Ввод невалидных данных ip-адреса (не в формате ipv4: 255.255.255.255);  
Указание флага без значения в командной строке (... -p -i 0 ... указан флаг -p, но не указано значение);  
Указание не предусмотренных флагов для нашей программы (-A, к примеру);  
Двойное указание флагов командной строки (... -p 123 -p 124 ...).  
  
Они все относятся к ошибкам при указании аргументов командной строки, так они указываются до выполнения программы один раз. Но программа не сможет выполняться с некорректными данными, следовательно, необходимо остановить выполнение программы.  
  
Некритичные ошибки:  
Введена некорректная команда при работе с cli-меню;  
Введен некорректный тип вектора в меню;  
Введено некорректное значение вектора в меню;  
Попытка ввести значения вектора до указания типа;  
W-компонента вектора = 0.  
  
Все эти ошибки возникают при работе пользователя с меню, так как в случае указания им некорректных данных, мы можем сообщить ему об ошибки и запросить ввод новых данных, эти ошибки некритичны.  
  
HOMETASK_03:  
1. Код приведен к структуре со схемы слайда;  
2. Был создан класс DataPool для хранения вводимых векторов;  
3. Были созданы классы Menu, MenuItem;  
4. Были созданы пробные классы для тестирования ресурсов и соединения;  
5. Классы для тестирования имеют абстрактного родителя (ровно как и типизированный вектор TypedVector в DataPool.h);  
6. Был создан класс AppSettings;  
7. Были прописаны инварианты классов.  
*  Тесты изменены под новую архитектуру.  
  
HOMETASK_04:  
1. Все функции принимают строки(std::string), вектора или объекты классов по ссылкам, что позволяет избежать копирования данных(более оптимизировано);  
2. Был добавлен класс NetworkAddress и внедрен в AppSettings. Класс принимает ip-адрес и порт через сеттеры(для ip перегрузка функции set_ip для разных форматов входных данных);  
3. Реализован метод класса для вывода сетевого адреса в стандартный поток вывода.  
4. В ветке прошлого домашнего задания(HOMETASK_03) был изменен метод для ввода вектора. Теперь при вызове команды "vector" программа ожидает получение одной строки из 4 значений вектора, а не 4х строк по одному значению.   
  
HOMETASK_05:  
1. В программе шаблоны используются в классе типизированного вектора(TypedVector), так как получаемый вектор может быть разных типов, то для работы с ним используются шаблоны (в файле DataPool.cpp/.h);  
2. В программе используются умные указатели (shared_ptr, unique_ptr);  
3. Выход по командам меню "exit"/"quit" (первая ставит флаг цикла меню в false, вторая просто выходит по exit() из cstdlib);  
4. В программе реализован метод класса menu - "parse_input" для отбрасывания непечатных символов и приведения букв к нижнему регистру(не используется для ввода имени, т.к. может, для имени нужно сохранение этих символов, а также регистра).  
  
HOMETASK_06:  
1. Структура menu_items_ в menu, хранящая "MenuItem"s, была изменена с vector на unordered_map, что позволяет получать функцию-обработчик команды за O(1), а не O(n) (так как раньше нужная команда искалась последовательно по элементам вектора, а теперь с помощью хэщ-таблицы);  
2. Был добавлен отельный класс для ведения логов, теперь более подробные логи об ошибках записываются в отдельный файл(logs/log.txt);  
3. (в процессе);  
4. Добавлен прототип сервера(необходимо добавить клиентскую часть для отправки запросов, логгер для сервера).  